# Cook It For Me

## Назначение

`Cook It For Me` — QoL-мод для **Project Zomboid Build 42**, рассчитанный только на одиночную игру. По правому клику в мире игрок открывает план готовки, выбирает блюдо и стратегию подбора еды; мод сам переносит предметы, заполняет посуду водой, собирает блюдо по ванильному evolved-рецепту, ставит его на плиту, следит за готовностью и забирает результат.

Мод не добавляет предметы или рецепты: он автоматизирует существующие ванильные механики и использует ванильные `TimedAction` / `RecipeManager` API.

## Быстрый сценарий

1. `CookItForMe_Client.lua` добавляет пункт контекстного меню.
2. `CookItForMe_PlanUI.lua` строит вкладки блюд и показывает план до запуска.
3. `Cook.plan()` сканирует окружение, выбирает посуду и продукты, проверяет evolved-рецепт и симулирует его на клонах для прогноза калорий/сытости.
4. `Cook.start()` ставит цепочку ванильных timed actions: взять посуду → вода (если нужна) → ингредиенты/специи → плита → нагрев.
5. `OnTick` отменяет процесс при зомби ближе 10 клеток, выключает плиту, когда блюдо готово, и переносит его в инвентарь.

## Поддерживаемые блюда

| Блюдо | Посуда | Вода |
| --- | --- | --- |
| Soup | `Base.Pot`, `Base.PotForged` | да |
| Stew | `Base.Pot`, `Base.PotForged` | да |
| Stir fry | `Base.Pan`, `Base.PanForged`, `Base.GridlePan` | нет |
| Roasted Vegetables | `Base.RoastingPan` | нет |

Нужны рабочая немикроволновая плита и совместимая посуда. Для супа и рагу также требуются раковина с водой. Сканирование идёт в радиусе настройки игрока; `0` — ближайший инвентарь/зона 3×3.

## Логика выбора еды

- Пропускаются не-еда, испорченное, приготовленное, сгоревшее и составные готовые блюда.
- Еда берётся из инвентаря игрока, контейнеров в радиусе и с пола; контейнеры на полу тоже учитываются.
- Стратегии: `max` — максимум калорий, `min` — минимум калорий, `hunger` — максимальная сытость.
- Не более двух ингредиентов одного `fullType`; лимит ингредиентов задаёт ванильный рецепт.
- Замороженные продукты разрешаются и увеличивают `minutesToCook`: `base * (1 + frozenPenalty * frozenCount)`, где по умолчанию `frozenPenalty = 0.5`.
- До четырёх специй: сначала до двух базовых (<10 ккал, по одному типу), затем подходящие выбранной стратегии.

## Карта файлов

```text
42/                         Build 42-версия мода
  mod.info                  ID CookItForMe, метаданные игры
  media/sandbox-options.txt Sandbox-флаг Debug
  media/lua/shared/
    CookItForMe_Shared.lua  глобальное состояние, ModData-настройки, debug-лог
    CookItForMe_Scanner.lua поиск плиты/раковины/контейнеров и фильтрация еды
    CookItForMe_FoodLogic.lua чистая, тестируемая логика подбора еды
    CookItForMe_Cook.lua    планирование и выполнение цикла готовки
  media/lua/client/
    CookItForMe_Client.lua  пункт контекстного меню
    CookItForMe_PlanUI.lua  окно плана, вкладки, настройки
  media/lua/server/
    CookItForMe_Server.lua  заглушка; серверная логика отсутствует
common/media/lua/shared/Translate/
  EN|RU/                    локализация UI и sandbox-настроек
tests/test_foodlogic.lua    офлайн-тесты чистой FoodLogic
workshop/                   Steam Workshop metadata и preview
tools/build_workshop.ps1    сборка структуры для ручной загрузки в Steam
```

## Состояние и события

- Настройки игрока: `player:getModData().CookItForMe`.
- Значения по умолчанию: `strategy=max`, `radius=1`, `frozenPenalty=0.5`; также хранятся позиция и размер окна.
- Sandbox: `CookItForMe.Debug` включает подробный вывод в `console.txt`; `debugFast` существует как скрытая отладочная настройка в ModData.
- События: `OnGameBoot`, `OnFillWorldObjectContextMenu`, `OnTick`.
- Флаг `Cook.active` глобален в Lua-сессии, что соответствует заявленному singleplayer-режиму. Не пытаться объявить мод мультиплеерным без переработки состояния и сетевой синхронизации.

## Разработка

Офлайн-тесты чистой логики (из каталога `tests`):

```powershell
lua test_foodlogic.lua
```

Сборка папки для uploader'а Steam:

```powershell
powershell -ExecutionPolicy Bypass -File tools\build_workshop.ps1
```

Скрипт создаёт `%USERPROFILE%\Zomboid\Workshop\CookItForMe\`, но не загружает мод: загрузка выполняется вручную из Project Zomboid → Workshop → Create/Update Item. Он сохраняет существующий Steam `id=` и удаляет выходную папку перед пересборкой.

## Правила изменений

- Не подменять ванильные действия собственными мгновенными мутациями инвентаря: цепочка намеренно использует `ISTimedActionQueue`.
- План и запуск должны использовать один и тот же объект `plan`: UI обещает игроку именно те ингредиенты, которые затем будут использованы.
- Новое блюдо требует синхронного изменения `DISHES`, локализаций и проверки имени ванильного evolved-рецепта в `matchRecipe()`.
- Изменения сканирования проверять в игре: часть API (`IsoStove`, вода, `RecipeManager`, inventory transfer) недоступна офлайн-тесту.
- Файлы Lua и переводов хранить в UTF-8. Русский текст в текущем выводе Windows PowerShell может отображаться неверно из-за кодовой страницы, поэтому не делать перекодирование вслепую.
